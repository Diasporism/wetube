var theaterUri = ("/theaters/"+$('#controls').data().theaterId);

$("#messages-container").append("<%= j render partial: 'messages/message', locals: {message: @message} %>");

$("#message-text-area").val("");
